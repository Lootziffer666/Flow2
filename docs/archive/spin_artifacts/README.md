# SPIN Prototype/Patch Artifacts (Archive)

This folder stores SPIN bundle artifacts (ZIPs) that are not canonical source trees.

Kept here for traceability:
- prototype bundles
- patch/security bundle archives

Canonical maintained SPIN code is in `packages/spin/`.
Active SPIN concept docs are in `docs/product/spin/`.
