Rename directory to "corpora". All necessary speech normalization und grammar corpora for real test patterns to evaluate , to iterate, to test and to harden the engine are stored in this directory. corpora_part_1.zip and corpora_part_2.zip were accidentally uploaded to root. Extract them into a logical file structure. (not just the zip files, extract the extracted zip files as well!). RUEG is in conll for English and German, all other files still need to be processed. litkey-Tab.xlsx is provided as separated parts to fit GitHub's upload restrictions and needs to be rebuild.
first, create a well structured database, then create a well suited test pattern suite provided as single file download containing multiple single Markdown files for FLOW/SPIN, benchmark my engine against those new patterns, then create a deep review of where we stand, what to do better, what the most valuable next three steps are, provide only a single file output, again. Maintain token capacity and context windows, use chain of thought to plan ahead.
