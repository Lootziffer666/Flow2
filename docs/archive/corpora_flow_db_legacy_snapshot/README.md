# Legacy `corpora/flow-db` Snapshot (Archived)

This folder preserves the former `corpora/flow-db/` starter bundle as a historical snapshot.

Why archived:
- Canonical FLOW DB runtime now lives in `flow-db/`.
- Keeping duplicate DB runtime code under `corpora/` created ownership ambiguity.

Status:
- Non-canonical historical reference only.
- Do not treat these files as runtime truth.
